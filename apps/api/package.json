{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "lint": "echo 'lint not configured yet'"
  },
  "dependencies": {
    "hono": "^4.7.2",
    "@clerk/backend": "^1.26.1"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
